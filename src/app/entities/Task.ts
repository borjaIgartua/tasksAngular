export class Task {
	// tslint:disable-next-line:indent
    constructor( public id: number,
                 public title: string,
                 public message: string,
                 public done: Boolean) { }
}